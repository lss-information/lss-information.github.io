name: Sync einsaetze.json

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download JSON
        run: |
          mkdir -p gameversions/nl_NL-Data
          curl -f -s -L http://meldkamerspel.com/einsaetze.json -o gameversions/nl_NL/nl_NL-Data/einsaetze.json


      - name: Commit if changed
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add gameversions/nl_NL-Data/einsaetze.json
          git commit -m "Auto update einsaetze.json" || echo "Nothing to commit"
          git push origin main
